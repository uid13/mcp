# 💾 内存使用详情

## 📊 内存分配

```mermaid
pie title 💾 内存分配 (总计 {{ totalGB }} GB)
    " 已用 {{ usedGB }}GB" : {{ usedPercent }}
    "🟢 空闲 {{ freeGB }}GB" : {{ freePercent }}
```

## 🔄 交换分区

| 项目 | 大小 |
|------|------|
|  总量 | {{ swapTotal }} GB |
| 🔴 已用 | {{ swapUsed }} GB |
| 🟢 空闲 | {{ swapFree }} GB |

## 🎴 内存条信息

| 插槽 | 容量 | 类型 | 频率 | 制造商 |
|------|------|------|------|--------|
{% for slot in slots %}
| 🎴 插槽{{ slot.index }} | {{ slot.size }} GB | {{ slot.type }} | {{ slot.clockSpeed }} MHz | {{ slot.manufacturer }} |
{% endfor %}
